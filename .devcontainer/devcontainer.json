{
  "// purpose": "One-command dev environment for TAU 3346. Brings up the full compose stack and",
  "// purpose2": "attaches to the FastAPI container with Python + Node tooling preinstalled, so a new",
  "// purpose3": "contributor can open the repo and start coding without local Postgres/Redis/Python setup.",
  "name": "TAU 3346 Dev",
  "dockerComposeFile": ["../docker-compose.dev.yml"],
  "service": "fastapi",
  "workspaceFolder": "/app",
  "features": {
    "ghcr.io/devcontainers/features/node:1": { "version": "20" }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff",
        "biomejs.biome",
        "bradlc.vscode-tailwindcss"
      ]
    }
  },
  "forwardPorts": [3000, 8000, 5432, 6379],
  "postCreateCommand": "pip install poetry && poetry install --no-root"
}
